services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: events
      POSTGRES_PASSWORD: events
      POSTGRES_DB: events
    ports:
      - "5432:5432"
    volumes:
      - ai_events_pgdata:/var/lib/postgresql/data

volumes:
  ai_events_pgdata:
